#
# Experimental and dangerous UMC hack
#
# - Overwrites part of the logic of
#   /etc/apachee2/sites-enabled/univention.conf
# - Generated by ansible
#
# Aims to "fake" the client IP to be always the same to circumvent the security
# checks of UMC's webserver.
#

<LocationMatch "^/univention/((auth|saml|get|set|command|upload|logout)/?.*)$">
    ProxyAddHeaders Off
    # Using an odd address, so that anyone seeing this would be irritated enough
    # to never consider using this line for anything real.
    RequestHeader set X-Forwarded-For 127.0.0.42
</LocationMatch>
