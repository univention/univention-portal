---

services:
  keycloak:
    extra_hosts:
      {{ ansible_fqdn }}: "{{ ansible_host }}"

  portal-consumer:
    extra_hosts:
      {{ ansible_fqdn }}: "{{ ansible_host }}"

  portal-consumer-init:
    extra_hosts:
      {{ ansible_fqdn }}: "{{ ansible_host }}"

  umc-server:
    extra_hosts:
      {{ ansible_fqdn }}: "{{ ansible_host }}"

...
