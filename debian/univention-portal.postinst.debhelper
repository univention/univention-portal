# Automatically added by univention-install-config-registry
if [ "$1" = "abort-remove" ]; then
  [ -e /etc/univention/templates/removed/univention-portal.info ] && mv /etc/univention/templates/removed/univention-portal.info /etc/univention/templates/info/univention-portal.info || true
fi
[ -x /usr/sbin/univention-config-registry ] && univention-config-registry register univention-portal || true
# End automatically added section

# run join script on Primary Directory Node and Backup Directory Node
if [ "$1" = "configure" ]; then
	. /usr/share/univention-lib/join.sh &&
		call_joinscript 33univention-portal.inst ||
		:
fi
# Automatically added by dh_installdeb/12.1.1
dpkg-maintscript-helper rm_conffile /etc/univention/templates/files/usr/share/univention-portal/apps.json 4.0.5-64~ -- "$@"
# End automatically added section
