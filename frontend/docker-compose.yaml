version: '3.0'

services:
  test:
    profiles:
      - test
    build:
      context: .
      target: test
    container_name: portal-frontend-test
    volumes:
      - .:/app
      - portal-frontend-dotcache:/root/.cache

volumes:
  portal-frontend-dotcache:
