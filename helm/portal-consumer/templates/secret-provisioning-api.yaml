{{/*
SPDX-FileCopyrightText: 2025 Univention GmbH
SPDX-License-Identifier: AGPL-3.0-only
*/}}

{{- /*
The registration document is always needed: `register-consumers` reads it from this Secret to
subscribe the consumer. Only the password half is optional - when `provisioningApi.auth.existingSecret`
names a Secret, the password is not rendered here and the document leaves it out, so the provisioning
chart's `registerConsumers.createUsers.<consumer>.passwordSecret` has to point at that same Secret.
*/}}
{{- $externalPassword := (.Values.provisioningApi.auth.existingSecret).name }}

{{- $secretName := include "nubus-common.secrets.name" ( dict "defaultNameSuffix" "provisioning-api" "context" . ) }}
{{- $username := tpl .Values.provisioningApi.auth.username . }}
{{- $password := "" }}
{{- if not $externalPassword }}
{{- $password = include "nubus-common.secrets.passwords.manage"
  (dict
    "secret" $secretName
    "key" "password"
    "username" "nubus"
    "site" "portal-consumer"
    "providedValues" (list "provisioningApi.auth.password")
    "context" .
  )
}}
{{- end }}
---
kind: "Secret"
apiVersion: "v1"
metadata:
  name: {{ $secretName | quote }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels:
    {{- include "common.labels.standard" ( dict "customLabels" .Values.additionalLabels "context" . ) | nindent 4 }}
  {{- $extraAnnotations := dict }}
  {{- if (.Values.global.secrets).keep }}
    {{- $_ := set $extraAnnotations "helm.sh/resource-policy" "keep" }}
  {{- end }}
  {{- include "nubus-common.annotations.render" ( dict
    "values" ( list $extraAnnotations .Values.additionalAnnotations )
    "context" . )
    | nindent 2 }}
stringData:
  {{- if not $externalPassword }}
  password: {{ $password | quote }}
  {{- end }}
  registration: |
    {
      "name": {{ $username | toJson }},
      "realms_topics": [
        {
          "realm": "udm",
          "topic": "groups/group"
        },
        {
          "realm": "udm",
          "topic": "portals/portal"
        },
        {
          "realm": "udm",
          "topic": "portals/category"
        },
        {
          "realm": "udm",
          "topic": "portals/entry"
        },
        {
          "realm": "udm",
          "topic": "portals/folder"
        },
        {
          "realm": "udm",
          "topic": "portals/announcement"
        }
      ],
      "request_prefill": false
      {{- if not $externalPassword }},
      "password": {{ $password | toJson }}
      {{- end }}
    }
