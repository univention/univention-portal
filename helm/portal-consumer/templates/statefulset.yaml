# SPDX-License-Identifier: AGPL-3.0-only
# SPDX-FileCopyrightText: 2023-2024 Univention GmbH

{{ $podSecurityContext := omit .Values.podSecurityContext "enabled" }}
{{ $containerSecurityContext := omit .Values.containerSecurityContext "enabled" }}
{{ $livenessProbe := omit .Values.probes.liveness "enabled" }}
{{ $readinessProbe := omit .Values.probes.readiness "enabled" }}

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "common.names.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels:
    {{- include "common.labels.standard" ( dict "customLabels" .Values.additionalLabels "context" $ ) | nindent 4 }}
  {{- if .Values.additionalAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.additionalAnnotations "context" . ) | nindent 4 }}
  {{- end }}
spec:
  serviceName: {{ include "common.names.fullname" . }}
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "common.labels.matchLabels" ( dict "customLabels" .Values.additionalLabels "context" $ ) | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/config-ucr: {{ include (print $.Template.BasePath "/configmap-ucr.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "common.labels.standard" ( dict "customLabels" .Values.additionalLabels "context" $ ) | nindent 8 }}
    spec:
      {{- if or .Values.imagePullSecrets .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- range .Values.global.imagePullSecrets }}
        - name: {{ . | quote }}
        {{- end }}
        {{- range .Values.imagePullSecrets }}
        - name: {{ . | quote }}
        {{- end }}
      {{- end }}
      {{- if .Values.podSecurityContext.enabled }}
      securityContext:
        {{- $podSecurityContext | toYaml | nindent 8 }}
      {{- end }}
      {{- if .Values.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- end }}
      {{- if .Values.serviceAccount.create }}
      serviceAccountName: {{ include "common.names.fullname" . }}
      {{- end }}
      initContainers:
        - name: wait-for-ldap
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext:
            {{- $containerSecurityContext | toYaml | nindent 12 }}
          {{- end }}
          command: ["/bin/bash", "/scripts/wait-for-ldap.sh"]
          image: "{{ coalesce .Values.waitForDependency.image.registry .Values.global.imageRegistry }}/{{ .Values.waitForDependency.image.repository }}:{{ .Values.waitForDependency.image.tag }}"
          imagePullPolicy: {{ coalesce .Values.waitForDependency.image.pullPolicy .Values.global.imagePullPolicy | quote }}
          env:
            {{- with .Values.waitForDependency.extraEnvVars }}
            {{- . | toYaml | nindent 12 }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "common.names.fullname" . }}
          volumeMounts:
            - name: scripts
              mountPath: /scripts
            - name: secret-ldap
              mountPath: /var/secrets/ldap_password
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.auth.existingSecret "key" "password" "context" .) | quote }}
              readOnly: true
            - name: secret-ldap
              mountPath: "/etc/machine.secret"
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.auth.existingSecret "key" "password" "context" .) | quote }}
              readOnly: true
            - name: secret-udm
              mountPath: /var/secrets/udm_api_password
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.udm.auth.existingSecret "key" "password" "context" .) | quote }}
            {{- if .Values.ldap.tls.enabled }}
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/ca_cert"
              subPath: ca.crt
              readOnly: true
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/cert_pem"
              subPath: cert.pem
              readOnly: true
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/private_key"
              subPath: tls.key
              readOnly: true
            {{- end }}
            {{- if .Values.waitForDependency.extraVolumeMounts }}
            {{- include "common.tplvalues.render" (dict "value" .Values.waitForDependency.extraVolumeMounts "context" .) | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.initResources | nindent 12 }}
        - name: wait-for-udm
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext:
            {{- $containerSecurityContext | toYaml | nindent 12 }}
          {{- end }}
          command: ["/bin/bash", "/scripts/wait-for-udm.sh"]
          image: "{{ coalesce .Values.waitForDependency.image.registry .Values.global.imageRegistry }}/{{ .Values.waitForDependency.image.repository }}:{{ .Values.waitForDependency.image.tag }}"
          imagePullPolicy: {{ coalesce .Values.waitForDependency.image.pullPolicy .Values.global.imagePullPolicy | quote }}
          env:
            {{- with .Values.waitForDependency.extraEnvVars }}
            {{- . | toYaml | nindent 12 }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "common.names.fullname" . }}
          volumeMounts:
            - name: scripts
              mountPath: /scripts
            - name: secret-ldap
              mountPath: "/var/secrets/ldap_password"
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.auth.existingSecret "key" "password" "context" .) | quote }}
              readOnly: true
            - name: secret-ldap
              mountPath: "/etc/machine.secret"
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.auth.existingSecret "key" "password" "context" .) | quote }}
              readOnly: true
            - name: secret-udm
              mountPath: /var/secrets/udm_api_password
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.udm.auth.existingSecret "key" "password" "context" .) | quote }}
            {{- if .Values.ldap.tls.enabled }}
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/ca_cert"
              subPath: ca.crt
              readOnly: true
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/cert_pem"
              subPath: cert.pem
              readOnly: true
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/private_key"
              subPath: tls.key
              readOnly: true
            {{- end }}
            {{- if .Values.waitForDependency.extraVolumeMounts }}
            {{- include "common.tplvalues.render" (dict "value" .Values.waitForDependency.extraVolumeMounts "context" .) | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.initResources | nindent 12 }}
        - name: wait-for-object-storage
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext:
            {{- $containerSecurityContext | toYaml | nindent 12 }}
          {{- end }}
          command: ["/usr/bin/python3", "/scripts/wait-for-object-store.py"]
          image: "{{ coalesce .Values.portalConsumer.image.registry .Values.global.imageRegistry }}/{{ .Values.portalConsumer.image.repository }}:{{ .Values.portalConsumer.image.tag }}"
          imagePullPolicy: {{ coalesce .Values.portalConsumer.image.pullPolicy .Values.global.imagePullPolicy | quote }}
          envFrom:
            - configMapRef:
                name: {{ include "common.names.fullname" . }}
          env:
            - name: OBJECT_STORAGE_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "defaultNameSuffix" "object-storage" "context" .) | quote }}
                  key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "key" "access_key_id" "context" .) | quote }}
            - name: OBJECT_STORAGE_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "defaultNameSuffix" "object-storage" "context" .) | quote }}
                  key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "key" "secret_access_key" "context" .) | quote }}
            {{- with .Values.waitForDependency.extraEnvVars }}
            {{- . | toYaml | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: scripts
              mountPath: /scripts
            {{- if .Values.waitForDependency.extraVolumeMounts }}
            {{- include "common.tplvalues.render" (dict "value" .Values.waitForDependency.extraVolumeMounts "context" .) | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.initResources | nindent 12 }}
        - name: wait-for-provisioning-api
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext:
            {{- $containerSecurityContext | toYaml | nindent 12 }}
          {{- end }}
          image: "{{ coalesce .Values.waitForDependency.image.registry .Values.global.imageRegistry }}/{{ .Values.waitForDependency.image.repository }}:{{ .Values.waitForDependency.image.tag }}"
          imagePullPolicy: {{ coalesce .Values.waitForDependency.image.pullPolicy .Values.global.imagePullPolicy | quote }}
          command: ["/bin/bash", "/scripts/wait-for-provisioning-api.sh"]
          volumeMounts:
            - name: scripts
              mountPath: /scripts
          env:
            - name: PROVISIONING_API_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.provisioningApi.auth.existingSecret "defaultNameSuffix" "provisioning-api" "context" .) }}
                  key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.provisioningApi.auth.existingSecret "key" "password" "context" .) }}
          envFrom:
            - configMapRef:
                name: {{ include "common.names.fullname" . }}
          resources:
            {{- toYaml .Values.initResources | nindent 12 }}
        - name: "univention-compatibility"
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext:
            {{- omit .Values.containerSecurityContext "enabled" | toYaml | nindent 12 }}
          {{- end }}
          image: "{{ coalesce .Values.portalConsumer.image.registry .Values.global.imageRegistry }}/{{ .Values.portalConsumer.image.repository }}:{{ .Values.portalConsumer.image.tag }}"
          imagePullPolicy: {{ coalesce .Values.portalConsumer.image.pullPolicy .Values.global.imagePullPolicy | quote }}
          envFrom:
            - configMapRef:
                name: {{ include "common.names.fullname" . }}
          env:
            - name: REQUESTS_CA_BUNDLE
              value: "/etc/ssl/certs/ca-certificates.crt"
            - name: OBJECT_STORAGE_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "defaultNameSuffix" "object-storage" "context" .) | quote }}
                  key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "key" "access_key_id" "context" .) | quote }}
            - name: OBJECT_STORAGE_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "defaultNameSuffix" "object-storage" "context" .) | quote }}
                  key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "key" "secret_access_key" "context" .) | quote }}
            - name: PROVISIONING_API_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.provisioningApi.auth.existingSecret "defaultNameSuffix" "provisioning-api" "context" .) }}
                  key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.provisioningApi.auth.existingSecret "key" "password" "context" .) }}
          command:
            - sh
            - -c
            - './50-ldap-setup.envsh; cp -a /etc/univention/* /target/etc/univention/'
          volumeMounts:
            - name: etc-univention-volume
              mountPath: /target/etc/univention
            - name: secret-ldap
              mountPath: "/var/secrets/ldap_password"
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.auth.existingSecret "key" "password" "context" .) | quote }}
              readOnly: true
            - name: secret-ldap
              mountPath: "/etc/machine.secret"
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.auth.existingSecret "key" "password" "context" .) | quote }}
              readOnly: true
            - name: secret-udm
              mountPath: /var/secrets/udm_api_password
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.udm.auth.existingSecret "key" "password" "context" .) | quote }}
            {{- if .Values.ldap.tls.enabled }}
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/ca_cert"
              subPath: ca.crt
              readOnly: true
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/cert_pem"
              subPath: cert.pem
              readOnly: true
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/private_key"
              subPath: tls.key
              readOnly: true
            {{- end }}
          resources:
            {{- toYaml .Values.initResources | nindent 12 }}
      containers:
        - name: "main"
          {{- if .Values.containerSecurityContext.enabled }}
          securityContext:
            {{- $containerSecurityContext | toYaml | nindent 12 }}
          {{- end }}
          image: "{{ coalesce .Values.portalConsumer.image.registry .Values.global.imageRegistry }}/{{ .Values.portalConsumer.image.repository }}:{{ .Values.portalConsumer.image.tag }}"
          imagePullPolicy: {{ coalesce .Values.portalConsumer.image.pullPolicy .Values.global.imagePullPolicy | quote }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" $livenessProbe "context" .) | nindent 12 }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" $readinessProbe "context" .) | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "common.names.fullname" . }}
          env:
            - name: REQUESTS_CA_BUNDLE
              value: "/etc/ssl/certs/ca-certificates.crt"
            - name: OBJECT_STORAGE_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "defaultNameSuffix" "object-storage" "context" .) | quote }}
                  key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "key" "access_key_id" "context" .) | quote }}
            - name: OBJECT_STORAGE_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "defaultNameSuffix" "object-storage" "context" .) | quote }}
                  key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.objectStorage.auth.existingSecret "key" "secret_access_key" "context" .) | quote }}
            - name: PROVISIONING_API_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.provisioningApi.auth.existingSecret "defaultNameSuffix" "provisioning-api" "context" .) }}
                  key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.provisioningApi.auth.existingSecret "key" "password" "context" .) }}
            {{- with .Values.extraEnvVars }}
            {{- . | toYaml | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: usr-lib-univention-portal-config-volume
              mountPath: /usr/lib/univention-portal/config
            - name: etc-univention-volume
              mountPath: /etc/univention
              readOnly: true
            - name: ucr
              mountPath: /etc/univention/base-forced.conf
              subPath: base-forced.conf
              readOnly: true
            - name: group-membership-cache
              mountPath: /usr/share/univention-group-membership-cache/caches
            - name: secret-ldap
              mountPath: /var/secrets/ldap_password"
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.auth.existingSecret "key" "password" "context" .) | quote }}
              readOnly: true
            - name: secret-ldap
              mountPath: "/etc/machine.secret"
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.auth.existingSecret "key" "password" "context" .) | quote }}
              readOnly: true
            - name: secret-udm
              mountPath: /var/secrets/udm_api_password
              subPath: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.udm.auth.existingSecret "key" "password" "context" .) | quote }}
            {{- if .Values.ldap.tls.enabled }}
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/ca_cert"
              subPath: ca.crt
              readOnly: true
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/cert_pem"
              subPath: cert.pem
              readOnly: true
            - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
              mountPath: "/var/secrets/private_key"
              subPath: tls.key
              readOnly: true
            {{- end }}
            {{- if .Values.extraVolumeMounts }}
            {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumeMounts "context" .) | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: "usr-lib-univention-portal-config-volume"
          emptyDir: {}
        - name: "etc-univention-volume"
          emptyDir: {}
        - name: "ucr"
          configMap:
            name: {{ printf "%s-ucr" (include "common.names.fullname" .) }}
        - name: "scripts"
          configMap:
            defaultMode: 0555
            name: {{ printf "%s-scripts" (include "common.names.fullname" .) }}
        - name: secret-ldap
          secret:
            defaultMode: 0400
            secretName: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.ldap.auth.existingSecret "defaultNameSuffix" "ldap" "context" .) | quote }}
        - name: secret-udm
          secret:
            defaultMode: 0400
            secretName: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.udm.auth.existingSecret "defaultNameSuffix" "udm" "context" .) | quote }}
        {{- if .Values.ldap.tls.enabled }}
        - name: {{ printf "%s-tls-volume" (include "common.names.fullname" .) }}
          secret:
            defaultMode: 0400
            secretName: {{ include "nubus-common.secrets.name" (dict "existingSecret" .Values.ldap.tls.existingSecret "defaultNameSuffix" "tls" "context" .) | quote }}
            items:
              - key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.tls.existingSecret "key" "ca.crt" "context" .) | quote }}
                path: ca.crt
              - key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.tls.existingSecret "key" "tls.crt" "context" .) | quote }}
                path: tls.crt
              - key: {{ include "nubus-common.secrets.key" (dict "existingSecret" .Values.ldap.tls.existingSecret "key" "tls.key" "context" .) | quote }}
                path: tls.key
        {{- end }}
        {{- if .Values.extraVolumes }}
        {{- include "common.tplvalues.render" (dict "value" .Values.extraVolumes  "context" .) | nindent 8 }}
        {{- end }}
        {{- if .Values.waitForDependency.extraVolumes }}
        {{- include "common.tplvalues.render" (dict "value" .Values.waitForDependency.extraVolumes  "context" .) | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  volumeClaimTemplates:
  - metadata:
      name: group-membership-cache
    spec:
      accessModes: [ "ReadWriteOnce" ]
      {{- if .Values.persistence.groupMembershipCache.storageClass }}
      storageClassName: {{ .Values.persistence.groupMembershipCache.storageClass | quote }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.persistence.groupMembershipCache.size | required ".Values.persistence.groupMembershipCache.size is required" | quote }}
