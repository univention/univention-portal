---
{{ include "common.deployment" (dict "top" . "overrides" "portal-frontend.deployment") }}
...

{{- define "portal-frontend.deployment" }}
spec:
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print .top.Template.BasePath "/configmap.yaml") .top | sha256sum | quote }}
{{- end }}
