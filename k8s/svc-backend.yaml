apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: univention-portal-backend
  ports:
    - name: http-backend
      protocol: TCP
      port: 8095
      targetPort: http-backend
