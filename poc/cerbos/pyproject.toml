# SPDX-License-Identifier: AGPL-3.0-only
# SPDX-FileCopyrightText: 2026 Univention GmbH

[project]
name = "cerbos-poc"
version = "0.1.0"
description = "PoC: portal tile visibility decided by Cerbos. Simulates the portal's filtering step against a real UDM and a local Cerbos."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "rich>=13.0",
]
