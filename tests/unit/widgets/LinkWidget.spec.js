// test if modelValueData is setCorrectly

// test if tabindex setting is working

// test if addField is working

// test if removeField is working

// test if LINK method is outputting correctly

// test if localeSelect method is outputting correctly

// test if linkInput method is outputting correctly

// test if removeLink method is outputting correctly


// Also: 

// :selected="modelValueData[index].locale || select"

// :name="index === 0 ? name : `${name}-${index}`"

// :aria-label="localeSelect(index)"

// :aria-label="linkInput(index)"